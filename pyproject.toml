[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bfv"
version = "1.0.0"
description = "Bayesian Fair Value Framework Engine"
dependencies = [
    "numpy",
    "scipy",
    "pandas"
]
